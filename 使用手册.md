# 使用手册（命令速查 · 部署 · 日常维护）

> 本手册记录了本项目所有常用命令和操作流程，方便随时查阅。
> 所有命令在 **Git Bash** 里执行（Windows 右键文件夹 → "Git Bash Here"）。

---

## 一、项目概览

| 项目 | 信息 |
|------|------|
| 本地路径 | `E:\AI-PJ\personal` |
| GitHub 仓库 | https://github.com/xuren9391/java-interview-kb |
| Gitee 仓库 | https://gitee.com/renxu/personal |
| 在线访问 | https://xuren9391.github.io/java-interview-kb/ |
| GitHub 用户名 | `xuren9391` |
| Gitee 用户名 | `renxu` |
| git 分支 | `master` |
| GitHub 推送方式 | **SSH**（已配好密钥，无需登录） |
| Gitee 推送方式 | HTTPS（已缓存凭证） |
| 代理端口 | `7892`（Clash，git 全局已配） |

---

## 二、日常工作流（最常用）⭐

### 完整循环：改内容 → 上线

```bash
cd /e/AI-PJ/personal        # 1. 进入项目目录
# （编辑 资料/ 面试/ 算法/ 里的 md 文件）
./build.sh push              # 2. 一键构建 + 提交 + 推送到 GitHub
```

推送后等 1-2 分钟，GitHub Pages 自动更新：
- 在线访问：https://xuren9391.github.io/java-interview-kb/

### 三种构建模式

| 命令 | 作用 |
|------|------|
| `./build.sh` | 只重新生成 `index.html`（本地预览，不上线） |
| `./build.sh push` | 生成 + 提交 + 推送到 **GitHub**（⭐ 最常用） |
| `./build.sh pushall` | 生成 + 提交 + 推送到 **GitHub + Gitee**（双平台同步） |

> 没改动时跑 `./build.sh push` 不会报错（会提示"没有变更需要提交"）。

---

## 三、Git 常用命令速查

### 基础操作

```bash
# 查看当前状态（哪些文件改了）
git status

# 查看所有本地分支
git branch

# 查看远程仓库地址
git remote -v

# 查看提交历史
git log --oneline -10
```

### 添加 / 提交 / 推送

```bash
# 添加所有改动
git add -A

# 提交（写清楚改了什么）
git commit -m "更新说明"

# 推送到 GitHub（SSH，无需登录）
git push github master

# 推送到 Gitee
git push origin master

# 同时推送到两个仓库
git push github master && git push origin master
```

### 查看差异

```bash
# 查看未暂存的改动
git diff

# 查看已暂存的改动
git diff --cached
```

### 撤销操作

```bash
# 撤销工作区的修改（还没 add）
git checkout -- <文件>

# 撤销已 add 但未 commit 的（保留改动在工作区）
git reset HEAD <文件>

# 撤销最近一次提交（保留改动）
git reset --soft HEAD~1
```

### 拉取远程更新（多电脑协作时）

```bash
# 从 GitHub 拉取
git pull github master

# 从 Gitee 拉取
git pull origin master
```

---

## 四、远程仓库配置

### 当前配置

```bash
# 查看远程
git remote -v
# 输出：
# github  git@github.com:xuren9391/java-interview-kb.git (SSH)
# origin  https://gitee.com/renxu/personal.git (HTTPS)
```

### 修改远程地址（备用）

```bash
# 改 GitHub（SSH 方式）
git remote set-url github git@github.com:xuren9391/java-interview-kb.git

# 改 GitHub（HTTPS 方式，需登录）
git remote set-url github https://github.com/xuren9391/java-interview-kb.git

# 改 Gitee
git remote set-url origin https://gitee.com/renxu/personal.git
```

---

## 五、代理配置（重要）

国内访问 GitHub 需要代理。git 代理已全局配置：

### 查看当前代理

```bash
git config --global http.proxy      # 应显示 http://127.0.0.1:7892
git config --global https.proxy
```

### 设置代理（如果换代理软件/端口）

```bash
# 把 7892 换成你代理软件的实际端口
git config --global http.proxy http://127.0.0.1:7892
git config --global https.proxy http://127.0.0.1:7892
```

### 取消代理（如果用 SSH 或不需要代理时）

```bash
git config --global --unset http.proxy
git config --global --unset https.proxy
```

> ⚠️ **注意**：SSH 方式（`git@github.com:...`）**不走 http 代理**，走 22 端口。如果 SSH 能通就不用管代理。HTTPS 方式才用代理。

### 常见代理端口参考

| 代理软件 | 默认 HTTP 端口 |
|---------|---------------|
| Clash / Clash Verge | 7890（或自定义，本项目是 7892）|
| v2rayN | 10809 |
| v2ray/SS 老版本 | 1080 |

---

## 六、SSH 密钥（已配好）

### 当前状态
- 密钥类型：`ed25519`
- 私钥：`~/.ssh/id_ed25519`（即 `C:\Users\Administrator\.ssh\id_ed25519`）
- 公钥：`~/.ssh/id_ed25519.pub`，已添加到 GitHub

### 测试 SSH 连接

```bash
ssh -T git@github.com
# 成功会显示: Hi xuren9391! You've successfully authenticated...
```

### 查看公钥（换电脑时需要重新添加）

```bash
cat ~/.ssh/id_ed25519.pub
```

### 重新生成密钥（换电脑或丢失时）

```bash
ssh-keygen -t ed25519 -C "xuren9391" -f ~/.ssh/id_ed25519 -N ""
cat ~/.ssh/id_ed25519.pub   # 复制输出，添加到 GitHub
```

添加到 GitHub：https://github.com/settings/ssh/new

---

## 七、项目结构

```
personal/
├── index.html              静态页（自动生成，双击可本地浏览）
├── build.js                构建脚本核心（Node）
├── build.sh                构建脚本（Git Bash 包装）
├── README.md               项目说明
├── .nojekyll               让 GitHub Pages 不忽略 _ 文件
├── .gitignore              git 忽略规则
│
├── 资料/                   知识体系（10 篇 md）
├── 面试/                   面试题集（12 篇 md）
└── 算法/                   算法专题（7 篇 md）
```

- **md 文件**：你编辑的源文件（资料/面试/算法 文件夹里）
- **index.html**：由 `build.js` 自动生成，**不要手动改**（会被覆盖）
- **build.js / build.sh**：构建工具，一般不用动

---

## 八、GitHub Pages 相关

### 在线地址
```
https://xuren9391.github.io/java-interview-kb/
```

### Pages 设置位置
```
https://github.com/xuren9391/java-interview-kb/settings/pages
```
- Source: Deploy from a branch
- Branch: `master` / `(root)`

### Pages 不更新怎么办？
1. 确认代码已推送：`git push github master`
2. 去 Pages 设置页看构建状态（绿条 = 正常）
3. 等 1-2 分钟（GitHub 构建有延迟）
4. 强制刷新浏览器：`Ctrl + F5`

### 强制刷新 CDN 缓存
- 浏览器 `Ctrl + F5`（或 `Cmd + Shift + R`）
- 或在地址后加参数：`https://xuren9391.github.io/java-interview-kb/?v=2`

---

## 九、本地预览

### 方式一：双击打开
直接双击 `E:\AI-PJ\personal\index.html`，用浏览器打开（断网也能用）。

### 方式二：起本地服务器（可选，热重载）
```bash
cd /e/AI-PJ/personal
python -m http.server 8000
# 浏览器访问 http://localhost:8000
```
按 `Ctrl + C` 停止。

---

## 十、常见问题排查

### Q1：`./build.sh push` 推送失败
```bash
# 检查 SSH 是否通
ssh -T git@github.com

# 检查远程地址
git remote -v

# 手动推送看报错
git push github master
```

### Q2：GitHub 网页打不开 / 推送超时
- 确认代理软件开着
- 检查代理端口：`git config --global http.proxy`
- SSH 方式不走 http 代理，如果 SSH 通就忽略代理问题

### Q3：Gitee 推送失败
- Gitee 用 HTTPS，需要凭证
- 如果凭证失效，重新登录或用 Token

### Q4：改了 md 但页面没变化
```bash
# 1. 确认重新构建了
./build.sh

# 2. 确认提交并推送了
git status              # 应该是 clean
git push github master

# 3. 等 1-2 分钟，浏览器 Ctrl+F5 强刷
```

### Q5：不小心改坏了 index.html
不用担心，index.html 是自动生成的，重新跑 `./build.sh` 就恢复了。

### Q6：想在另一台电脑上继续
```bash
# 新电脑上：
git clone git@github.com:xuren9391/java-interview-kb.git
cd java-interview-kb
# 配好 SSH key 和 Node.js，就可以继续用 ./build.sh push 了
```

---

## 十一、环境要求

| 工具 | 要求 | 检查命令 |
|------|------|---------|
| Git | 已安装 | `git --version` |
| Git Bash | 已安装（Windows） | 右键应有 "Git Bash Here" |
| Node.js | v14+ | `node --version` |
| SSH | 已配好密钥 | `ssh -T git@github.com` |

---

## 十二、速查卡片（打印贴墙上）

```
┌─────────────────────────────────────────────┐
│  改完内容上线（最常用）：                      │
│    cd /e/AI-PJ/personal                      │
│    ./build.sh push                            │
│                                              │
│  在线访问：                                    │
│    https://xuren9391.github.io/java-interview-kb/ │
│                                              │
│  本地预览：双击 index.html                     │
│                                              │
│  查看状态：git status                         │
│  手动推送：git push github master             │
└─────────────────────────────────────────────┘
```
