# 高频题集（按专题 · Java 完整解法 · 复杂度分析）

> 精选 **LeetCode Hot 100 + 剑指 Offer 高频题**，按专题分类，每题给「思路 + Java 代码 + 复杂度」。
> 建议按顺序刷，每题先自己写 15-30 分钟，超时再看答案，看完**关闭答案默写一遍**。

---

# 一、数组 / 双指针

## LC 1 两数之和（哈希，Easy）
**思路**：遍历，每个数查 `target - num` 在不在哈希表。
```java
public int[] twoSum(int[] nums, int target) {
    Map<Integer,Integer> map = new HashMap<>();
    for (int i = 0; i < nums.length; i++) {
        int need = target - nums[i];
        if (map.containsKey(need)) return new int[]{map.get(need), i};
        map.put(nums[i], i);
    }
    return new int[]{};
}
```
时间 O(n)，空间 O(n)。

## LC 15 三数之和（排序+双指针，Medium）
见算法/02。排序后固定一个数，另两个用对撞双指针。去重要小心。

## LC 11 盛最多水的容器（双指针，Medium）
见算法/02。`max = max(max, min(h[l],h[r]) * (r-l))`，移动较矮的。

## LC 283 移动零（快慢指针，Easy）
见算法/01。slow 指向下一个非零位置。

## LC 42 接雨水（Hard，多种解法）
**双指针法**（推荐）：
```java
public int trap(int[] h) {
    int l = 0, r = h.length - 1, lMax = 0, rMax = 0, ans = 0;
    while (l < r) {
        if (h[l] < h[r]) {
            if (h[l] >= lMax) lMax = h[l]; else ans += lMax - h[l];
            l++;
        } else {
            if (h[r] >= rMax) rMax = h[r]; else ans += rMax - h[r];
            r--;
        }
    }
    return ans;
}
```
也可用动态规划（预处理左右最大值）或单调栈。

---

# 二、链表

## LC 206 反转链表（Easy，必背）
见算法/01。

## LC 21 合并两个有序链表（Easy）
见算法/01。dummy + 双指针。

## LC 141/142 环形链表（Easy/Medium）
快慢指针判环；找入口：相遇后一个从头出发，再相遇即入口。

## LC 19 删除倒数第 N 个节点（Medium）
快慢指针：fast 先走 n 步，再一起走。
```java
public ListNode removeNthFromEnd(ListNode head, int n) {
    ListNode dummy = new ListNode(-1); dummy.next = head;
    ListNode fast = dummy, slow = dummy;
    for (int i = 0; i < n; i++) fast = fast.next;
    while (fast.next != null) { fast = fast.next; slow = slow.next; }
    slow.next = slow.next.next;
    return dummy.next;
}
```

## LC 160 相交链表（Easy）
双指针走完自己的再走对方的，相遇即交点。

## LC 148 排序链表（Medium，归并）
找中点（快慢）→ 拆两半 → 分别排序 → 合并。O(n log n)。

## LC 23 合并 K 个升序链表（Hard，堆）
用小根堆，每次弹最小头。O(n log k)。

---

# 三、栈 / 队列

## LC 20 有效的括号（Easy，必背）
见算法/01。

## LC 155 最小栈（Medium）
辅助栈同步存当前最小值。

## LC 394 字符串解码（Medium，栈）
`3[a2[c]]` → `accaccacc`。遇 `]` 弹出处理，遇 `[` 数字/字符串入栈。

## LC 739 每日温度（Medium，单调栈）
见算法/03。栈存下标，遇到更高温就弹出结算。

---

# 四、哈希 / 字符串

## LC 49 字母异位词分组（Medium）
key = 排序后的字符串（或 26 字母计数）。
```java
public List<List<String>> groupAnagrams(String[] strs) {
    Map<String, List<String>> map = new HashMap<>();
    for (String s : strs) {
        char[] c = s.toCharArray(); Arrays.sort(c);
        map.computeIfAbsent(new String(c), k -> new ArrayList<>()).add(s);
    }
    return new ArrayList<>(map.values());
}
```

## LC 128 最长连续序列（Medium，O(n)）
```java
public int longestConsecutive(int[] nums) {
    Set<Integer> set = new HashSet<>();
    for (int n : nums) set.add(n);
    int max = 0;
    for (int n : set) {
        if (!set.contains(n - 1)) {       // 只从序列起点开始
            int cur = n, len = 1;
            while (set.contains(cur + 1)) { cur++; len++; }
            max = Math.max(max, len);
        }
    }
    return max;
}
```

## LC 3 无重复字符的最长子串（Medium，必背）
见算法/02（滑动窗口）。

## LC 5 最长回文子串（Medium，中心扩展）
```java
public String longestPalindrome(String s) {
    int start = 0, max = 0;
    for (int i = 0; i < s.length(); i++) {
        int len1 = expand(s, i, i), len2 = expand(s, i, i + 1);
        int len = Math.max(len1, len2);
        if (len > max) { max = len; start = i - (len - 1) / 2; }
    }
    return s.substring(start, start + max);
}
int expand(String s, int l, int r) {
    while (l >= 0 && r < s.length() && s.charAt(l) == s.charAt(r)) { l--; r++; }
    return r - l - 1;
}
```

---

# 五、二叉树（递归）

## LC 104 二叉树最大深度（Easy）
`1 + max(left, right)`。

## LC 226 翻转二叉树（Easy）
交换左右子树。

## LC 543 二叉树直径（Easy，后序）
后序求左右深度，直径 = 左深 + 右深。

## LC 236 最近公共祖先（Medium，经典）
见算法/01。

## LC 102 二叉树层序遍历（Medium，BFS）
见算法/01。

## LC 98 验证 BST（Medium，中序）
见算法/01。

## LC 124 二叉树最大路径和（Hard，后序）
```java
int max = Integer.MIN_VALUE;
public int maxPathSum(TreeNode root) {
    dfs(root); return max;
}
int dfs(TreeNode root) {
    if (root == null) return 0;
    int left = Math.max(0, dfs(root.left));
    int right = Math.max(0, dfs(root.right));
    max = Math.max(max, left + right + root.val);   // 以当前为最高点的路径
    return Math.max(left, right) + root.val;        // 只能选一边往上
}
```

---

# 六、二分查找

## LC 704 二分查找（Easy）
见算法/02。

## LC 33 搜索旋转排序数组（Medium，必背）
见算法/02。

## LC 34 在排序数组中查找元素第一个和最后一个位置（Medium）
两次二分（lowerBound + upperBound）。

## LC 153 寻找旋转排序数组中的最小值（Medium）
二分：`nums[mid]` 与 `nums[r]` 比较。

## LC 162 寻找峰值（Medium）
二分：往上升的方向走。

---

# 七、动态规划

## LC 70 爬楼梯（Easy）
`dp[i] = dp[i-1] + dp[i-2]`。

## LC 198 打家劫舍（Medium）
`dp[i] = max(dp[i-1], dp[i-2]+nums[i])`。

## LC 300 最长递增子序列（Medium）
O(n²) DP 或 O(n log n) 二分。

## LC 1143 最长公共子序列（Medium）
见算法/03。

## LC 72 编辑距离（Hard）
见算法/03。

## LC 322 零钱兑换（Medium，完全背包）
见算法/03。

## LC 121 买卖股票最佳时机（Easy）
维护最小价 + 最大利润。

## LC 152 乘积最大子数组（Medium）
维护当前最大/最小（负数反转）。

## LC 139 单词拆分（Medium）
`dp[i]` = 前 i 个字符能否拆分。

## LC 64 最小路径和（Medium，网格 DP）
`dp[i][j] = min(dp[i-1][j], dp[i][j-1]) + grid[i][j]`。

---

# 八、回溯

## LC 46 全排列（Medium，必背）
见算法/03。

## LC 78 子集（Medium）
见算法/03。

## LC 39 组合总和（Medium）
见算法/03。

## LC 22 括号生成（Medium）
回溯，限制左右括号数量。

## LC 79 单词搜索（Medium）
DFS + 回溯（标记访问）。

## LC 51 N 皇后（Hard）
见算法/03。

## LC 131 分割回文串（Medium）
回溯 + 判断回文。

---

# 九、图 / BFS / DFS

## LC 200 岛屿数量（Medium，必背）
见算法/03。

## LC 207 课程表（Medium，拓扑排序）
见算法/03。

## LC 994 腐烂的橘子（Medium，BFS）
多源 BFS，求所有橘子腐烂的最少分钟。

## LC 207/210 课程表 I/II（拓扑）
BFS + 入度。

---

# 十、堆 / Top K

## LC 215 第 K 个最大元素（Medium，必背）
小根堆（大小 k）或快速选择。

## LC 347 前 K 个高频元素（Medium）
见算法/01。

## LC 295 数据流的中位数（Hard）
大根堆存左半 + 小根堆存右半，平衡两堆大小。

---

# 十一、其他高频

## LC 146 LRU 缓存（Medium，手撕必背）
见算法/05。

## LC 215 见上。

## LC 169 多数元素（Easy，Boyer-Moore 投票）
```java
public int majorityElement(int[] nums) {
    int candidate = 0, count = 0;
    for (int n : nums) {
        if (count == 0) candidate = n;
        count += (n == candidate) ? 1 : -1;
    }
    return candidate;
}
```

## LC 283/11/42 见上。

## LC 53 最大子数组和（Easy，必背）
```java
public int maxSubArray(int[] nums) {
    int max = nums[0], cur = nums[0];
    for (int i = 1; i < nums.length; i++) {
        cur = Math.max(cur + nums[i], nums[i]);   // 续 or 重开
        max = Math.max(max, cur);
    }
    return max;
}
```

---

# 十二、刷题统计建议 🔴

**面试前必刷清单（按优先级）**：
1. **LC Hot 100**（https://leetcode.cn/studyplan/top-100-liked/）—— 必刷
2. **剑指 Offer**（https://leetcode.cn/problemset/2021/）—— 必刷
3. **面试经典 150 题**（https://leetcode.cn/studyplan/top-interview-150/）
4. **Codetop 按公司查**（https://codetop.cc/）—— 面试前定向刷

**目标**：12 周刷完 100 题核心 + 持续保持。

> 配套：算法/05（手撕设计题，下一份）、算法/06（大厂真题）
